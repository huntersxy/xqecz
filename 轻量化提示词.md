# 轻量化提示词（xqecz 仓库）

## 一、省 token 工作模式（贴到会话开头）

```text
接下来在这个仓库工作时，请全程保持省 token 模式：
1. 先定位再读取：用 rg / Select-String 找符号，只读相关行区间，不要整文件输出超过 200 行的文件。
2. 最小上下文：只读 AGENTS.md、前端契约 packages/frontend/src/api/index.ts 以及本次改动涉及的文件。
3. 忽略噪音：node_modules、dist、build、pnpm-lock.yaml、proto/gen 生成代码、.git。
4. 输出精简：只给 diff 和结论，commentary 不超过 3 句，不重复已知信息。
5. 验证只跑本次相关的 typecheck / 单测，不全量 build。
```

## 二、轻量化改造任务（保持功能不变）

```text
请对本仓库做一次轻量化改造，保持所有功能、接口、数据结构不变：
1. 先用脚本统计仓库规模，列出最大的 10 个手写文件；
2. 按安全顺序处理：死代码 → 重复代码 → 冗余 → 必要的大文件拆分；
3. 每类改动先展示计划和预计删减行数，再小步实施，每步跑对应包的 typecheck / 测试；
4. 最终汇报 git diff --stat 和净删减行数。
```

## 三、使用方法

- **方式 A（零安装）**：把第一段提示词贴在每次会话开头，Codex 就会按省 token 模式工作。
- **方式 B（自动触发）**：安装配套 skill `codebase-lean`，之后说「让项目轻量化」「省着点 token」即可自动按流程执行（含统计脚本与项目红线）。

## 四、安装 skill（可选）

把仓库内的技能复制到个人技能目录即可自动发现：

```powershell
Copy-Item -Recurse D:\xqecz\xqecz\.codex\skills\codebase-lean C:\Users\xiey\.codex\skills\codebase-lean
```

复制后重启 Codex。仓库内保留一份副本，方便其他协作者或换机器时同步。
